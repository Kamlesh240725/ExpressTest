<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white my-5 mx-10">
    <nav class="mb-10 text-blue-300">
        <a href="/register">Create Users</a>
    </nav>
    <% if (allUsers.length<=0) { %>
        <h1 class="font-medium text-2xl tracking-tight">No Existing Users</h1>
    <% } else { %>       
        <h1 class="font-medium text-2xl tracking-tight">Read Users</h1>
        <div class="users w-[100%] flex flex-wrap mt-10 gap-4">
            <% allUsers.forEach(user => { %>   
                <div class="user min-w-48 max-w-52 bg-zinc-700 flex flex-col items-center py-10 rounded-md relative">
                    <img src="<%= user.img_URL %>" alt="user" class="w-24 h-24 rounded-[100%]">
                    <h2 class="font-bold text-lg font-medium mt-2"><%= user.name %></h2>
                    <p class="text-sm text-zinc-300"><%= user.email %></p>
                    <a href="/edituser/<%= user.name %>" class="absolute bottom-1 left-2 text-sm text-zinc-400">Edit User</a>
                    <a href="/deleteuser/<%= user.name %>" class="absolute bottom-1 right-2 text-sm text-red-400">Delete User</a>
                </div>        
            <% }); %>
        </div>
    <% } %>
</body>
</html>